
function copyObj(obj, arrOfKeys) {
  if (arrOfKeys = undefined) {
    arrofKeys = 
  }
  let cloneObj = Object.create(obj.prototype);
}

//I couldn't answer this one, but tried. Still need to work on arrays and objects!!!!